import { allowlistTokens } from "./allowlist";

async function main() {
  const ownerCounts: Record<string, number> = {};

  for (const { type, contract, tokenIds } of allowlistTokens) {
  }
}

main();
